import {ADD_TOKEN} from './actions'

const initialState = {
    token: null
}

function alltesterApp(state, action) {
    if (typeof state === 'undefined') {
      return initialState
    }
  ​
    return state
}