
import React from 'react';

const Stopwatch = () => (
    <div>
        <p>0ms</p>
        <button
            onClick={null}
            className="button">click</button>
    </div>
    );

export default Stopwatch;
